# Example values - do not commit real secrets
# The pipeline can auto-generate the DB password, so most teams only set region or secret_name.

# aws_region  = "us-east-1"
# secret_name = "task3/rds/postgres"

# Optional override if you do not want Terraform to auto-generate the DB password:
# db_password = "ChangeMeStrongPassword123!"
