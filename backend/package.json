{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "check": "node --check app.js && node --check routes/api.js && node --check config/db.js && node --check config/secrets.js && node --check services/dbService.js"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1016.0",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "pg": "^8.20.0"
  }
}
